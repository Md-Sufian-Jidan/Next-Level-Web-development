// Javascript dise object and array

// same for typescript

const numbers: number[] = [1, 2, 3, 4, 5, 6, 7, 8, 9];

// numbers.push('s'); // error dibe 'string' is not assignable to parameter of type 'number'

numbers.push(10); // right value tai insert korte dibe